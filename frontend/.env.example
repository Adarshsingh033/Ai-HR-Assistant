# =============================================================================
# AI HR Assistant – Frontend Environment Variables
# =============================================================================
# The frontend auto-detects the API URL from the browser's origin.
# Only set this if you need to override it (e.g., separate API server).
# =============================================================================

# API Base URL (leave empty to auto-detect from browser origin)
API_BASE_URL=
